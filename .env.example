# Upstash Redis (Vercel Marketplace 연동 시 자동으로 주입됩니다)
# 두 값이 모두 없으면 앱은 자동으로 브라우저 localStorage 저장소로 동작합니다.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Vercel KV(레거시) 연동 시 이름이 다르게 주입되는 경우를 위한 별칭 (선택)
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
